const preRoute = '';
const preAdmin = '/admin';

export const ROUTES = {

}

export const ROUTES_ADMIN = {
    ADMIN:`${preAdmin}`,
    DASHBOARD: `${preAdmin}/home`,
    CUSTOMER_MANAGEMENT: `${preAdmin}/customer-management`,
    PRODUCT_MANAGEMENT: `${preAdmin}/product-management`,
    ORDER: `${preAdmin}/order`,
}